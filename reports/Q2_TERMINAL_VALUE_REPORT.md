# 第二问：固定购电的终端估值对照

## 当前状态

实现及短期验收已完成；全年新估值结果尚待完成，不作全年收益结论。

- 原固定线性终端的全年重放：334天、48096段，控制决策与原50 kWh增强模型在1e-6容差内一致；费用13989089.105113324元。
- 新增6项测试通过：覆盖线性兼容、常数平移、效率、插值、失败回退、历史因果性及边界执行。
- 新估值7天试跑：1008段，费用338240.665767273元，未利用量48626.535436082784 kWh，曲线回退0天。新旧充放电及SOC在1e-6容差内一致，未观察到改善。
- 全年重放及短期试跑均已通过独立物理、费用、固定购电与来源哈希校验。

## 模型边界

固定原193点增强模型的普通购电，只替换Bellman终端值；下一日估值包含25个400 kWh间隔储能点和7条历史误差情景。虚拟下一日购电跨情景共享，不执行；未来充放电独立补救属于偏乐观两阶段近似，不是场景树。

每天0点只用此前历史构造曲线，当天不更新曲线；当前控制的6小时权重更新保持不变。下一日末端沿用固定线性估值，12月30日估值要求下一日末端1200 kWh，12月31日保留年度闭合。终端估值不计入真实费用。

输入数据错误停止；无有效估值解时整条曲线回退。求解限时60秒，相对gap0.001。求解容差可能造成曲线数值噪声，不强制单调化。

## 复现

使用已安装依赖的Python执行 `code/finish_c_q2_terminal.py`，依次运行全年、独立校验和图表报告生成。结果写入 `results/q2_terminal_value/`；逐日台账和曲线提前落盘，汇总在全年成功后生成。图表生成后仍需视觉检查。

短期结果在 `tmp/q2_terminal_week/`，原控制重放在 `tmp/q2_terminal_replay/`。未修改旧版模型源码及结果，未提交或推送Git。
